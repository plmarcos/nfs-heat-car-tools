# Fim de linha normalizado, para o diff nao encher de ruido em quem clonar fora
# do Windows. O `.ps1` e' a excecao: o PowerShell 5.1 le' melhor em CRLF, e estes
# scripts sao a parte que so' roda no Windows mesmo.
* text=auto eol=lf

*.ps1 text eol=crlf
*.bat text eol=crlf
*.cs  text eol=crlf
